#include "main.h"

/**
 * add - adds two integers and give result
 * @i: int to be added to x
 * @x: int to be added to i
 * Return: sum of i and x
 */

int add(int i, int x)
{
	int y;

	y = i + x;

	return (y);
}
