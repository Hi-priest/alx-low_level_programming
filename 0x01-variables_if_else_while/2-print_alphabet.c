#include <stdio.h>

/**
 * main - pogram prints alphabets in lower cases
 *
 * Return: Answer 0 (Success)
 */
int main(void)
{
	char c = 'a';

	while
		(c <= 'z');
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
