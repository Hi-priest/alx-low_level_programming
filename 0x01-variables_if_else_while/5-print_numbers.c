#include <stdio.h>

/**
 * main - program prints single digit number of base 10 from 0
 *
 * Return: 0 (success)
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
